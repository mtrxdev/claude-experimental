# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a fresh experimental repository (`claude-experimental`) with no source code, build system, or tooling configured yet. The only file currently tracked is `README.md`.

Update this file as the project takes shape — add build commands, architecture notes, and conventions once they're established.
